<style>
  ::-webkit-scrollbar-thumb {
    background: #666666;
    border-radius: 5px
  }

  ::-webkit-scrollbar-track {
    background: #fff;
    border-radius: 5px
  }

  ::-webkit-scrollbar {
    width: 6px;
    height: 6px;
    background: #fff;
  }

  body {
    font-size: 16px;
    font-family: "Segoe UI", "Microsoft YaHei", Helvetica, sans-serif, Arial;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    margin: 0;
  }

  .back {
    opacity: 0.6;
    position: fixed;
    z-index: -1;
    top: 0;
    width: 100%;
  }

  .back img {
    width: 100%;
  }

  #top {
    min-height: 10vw;

  }

  #top::before {
    display: inline-block;
    position: absolute;
    z-index: -1;
    width: 100%;
    height: 100%;
    content: "";
    background-image: url("<%- url_for(theme.images.top) %>");
    background-size: cover;
    opacity: 0.5;
    overflow: hidden;
  }
</style>